ExitPlanMode nie jest dostępne w tej sesji, więc plan zostaje w pliku `/Users/jakubgawronski/.claude/plans/jeste-planist-dostajesz-jedno-swift-frog.md`. Skrót:

## 1. Pliki

- **`src-tauri/tests/it/t147_startup_reaper_refuses_ambiguous_probe.rs`** (nowy) — dokładna kopia treści, bez zmian: nagłówek `//!`, dwa testy, helper `scripted_reap`, te same asercje i komunikaty.
- **`src-tauri/tests/t147_startup_reaper_refuses_ambiguous_probe.rs`** — usunięty; przenosiny przez `git mv`, żeby diff pokazał R100.
- **`src-tauri/tests/it/main.rs`** — `mod t147_startup_reaper_refuses_ambiguous_probe;` **między** linią 279 (`mod t115_unknown_codex_price_stays_unknown;`) i 280 (`mod the_index_says_what_each_file_is;`). Pozycja jest wymuszona: nie ma `rustfmt.toml`, więc `reorder_modules` jest domyślnie włączone i wpis poza porządkiem zapali `cargo fmt --check`.

Plik nie używa `tests/support/` — importuje tylko `VecDeque`, `Duration` i `loadout_lib::engine::supervisor`, więc żadna ścieżka importu nie wymaga dostosowania. `reap_group_with_signaler` (`supervisor.rs:1999`) jest czystą funkcją sterowaną domknięciem — bez `env::set_var`, liczenia deskryptorów i haków paniki, czyli nie podpada pod wyjątek „stan całego procesu" z nagłówka `it/main.rs`. Nazwa celu nie występuje w treści ani jednego pliku w repo: żaden check, `checks.json` ani `ci.sh` jej nie cytuje, a `Cargo.toml` nie ma sekcji `[[test]]`, więc cel zniknie sam.

## 2. Akceptacja

1. `cargo test --test it t147_startup_reaper_refuses_ambiguous_probe::` melduje `2 passed`; dziś ta sama komenda melduje `0 passed`.
2. `cargo test --test t147_startup_reaper_refuses_ambiguous_probe` przewraca się z `error: no test target named …` — dowód, że binarium zniknęło, a nie że doszedł drugi egzemplarz testów.
3. `bash checks/tests-listed.sh` wypisuje `tests-listed: 290 test files, every one declared in main.rs` (dziś 289), bez linii `these test files are not declared`.
4. `ls -1 src-tauri/tests/*.rs | wc -l` daje 59 (dziś 60), a łączna liczba testów w `cargo test` jest identyczna jak przed zmianą.

## 3. Test

**TEST: brak.** Zakres zabrania nowego testu („liczba testów i ich asercje się nie zmieniają"), więc każdy dopisany test byłby wyjściem z zadania. Rolę testu padającego przed zmianą gra punkt 1 akceptacji: `0 passed` → `2 passed`, czyli dokładnie ten oracle (licznik przejść, niezmiennik 19), którym bramka sądzi każde kryterium rustowe.

---

POZA ZAKRESEM: `docs/HARNESS-QUEUE.md` Q-7 mówi „przeniesc 60 istniejacych celow" — po tej zmianie zostaje 59, ale zakres zabrania dotykania czegokolwiek poza dwoma plikami.