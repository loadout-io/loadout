# Z-29 — Fikstury bez prywatnego środowiska

docs/research/fixtures/claude-stream.jsonl i claude-partial.jsonl niosą wiersz system/init właściciela: ścieżki domowe, listę serwerów MCP (Gmail, Calendar, Drive), ścieżki pluginów, socket, session_id, request_id. Repo jest publiczne. Przepisz skryptem: kształt zdarzeń zostaje, ścieżki i identyfikatory na REDACTED, lista mcp_servers i plugins skrócona do jednego wpisu; Plik `docs/PROMPT-FAZA-7-CODEX.md` jest JUŻ załatwiony: pakiet 0.5 przeniósł go do
`docs/archive/` ze ścieżką domową zamienioną na `<korzeń repo>` (dziś 0 trafień).
Zostają dwie fikstury w `docs/research/fixtures/` — po 3 trafienia `jakubgawronski`
każda, plus lista serwerów MCP właściciela, ścieżki pluginów, gniazdo i identyfikatory
sesji w wierszu `system/init`. Uruchom testy, które czytają te fikstury, i pokaż, że liczba passed jest ta sama.

## Kryterium akceptacji (z audytu 2026-09-02)

grep -rn '/Users/jakubgawronski\|kgm004a' docs/ src/ src-tauri/ = 0; testy czytające fikstury zielone.

## Źródła w audycie

R-6

## Reguły tego zadania

- Przeczytaj AGENTS.md; niezmienniki cytuj po numerze.
- **`TEST: brak` jest tu właściwą odpowiedzią** i wymaga jednego zdania uzasadnienia:
  to redakcja danych w fiksturach, nie zmiana zachowania. Dowodem jest to, że testy
  czytające te pliki mają tę samą liczbę `passed` przed i po — policz ją.
- Nie ruszaj harness/, checks/, scripts/, .claude/, AGENTS.md, docs/DECISIONS-LOCKED.md. Jeśli kryterium wymaga ich zmiany, powiedz to i zatrzymaj się.
- Zmieniaj tylko pliki potrzebne do tego zadania; inne problemy wypisz jako POZA ZAKRESEM.
- Komentuj DLACZEGO z datą 2026-09 przy nieoczywistej linii (niezmiennik 24).
