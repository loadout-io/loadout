# Z-32 — Biblioteka: zgodność wersji, slugi, nazwy, liczby

library/agents.rs i workflow/mod.rs: (1) deny_unknown_fields na Agent zamienia każde nowe pole z nowszego buildu w DefinitionProblem w starszym (wydanie i build deweloperski dzielą ~/.loadout) — nieznane pola do extra jak w workflow, ostrzeżenie o literówce tylko dla kluczy o odległości Levenshteina ≤ 2 od znanych; (2) kolizje slugów (Forge/forge, Żółw → w.md) kończą się mylącym file changed on disk — porównaj slug ze zdrowymi definicjami o innym id i odmów własnym zdaniem, przy kolizji sufiks z ośmioma znakami id; (3) zmiana nazwy zostawia stary plik — po publikacji usuń inne pliki .md z tym samym id pod tym samym uchwytem katalogu; (4) copies i max_turns jako u8 robią z 300 cały plik Malformed — u32 w pliku, walidacja osobno z uwagą przy kafelku; (5) nazwy umiejętności w roster.rs i workflows.rs porównuj eq_ignore_ascii_case jedną funkcją; (6) drobiazgi: create_dir_all po walidacji, settings.json przez ten sam durable_file z rewizją, scalar() nie robi liczby z name. Osobny test na każdy punkt.

## Kryterium akceptacji (z audytu 2026-09-02)

Agent z nieznanym polem czyta się ze zdaniem o nowszym Loadoucie, nie jako Malformed; Forge/forge = odmowa własnym zdaniem; zmiana nazwy nie zostawia drugiego pliku; max_turns: 300 = uwaga przy kafelku; nazwy umiejętności bez wielkości liter.

## Źródła w audycie

W-9, W-11, W-12, W-13, W-16, W-17

## Reguły tego zadania

- Przeczytaj AGENTS.md; niezmienniki cytuj po numerze.
- Test z sekcji Test planu ma paść na starym kodzie zanim powstanie poprawka.
- Nie ruszaj harness/, checks/, scripts/, .claude/, AGENTS.md, docs/DECISIONS-LOCKED.md. Jeśli kryterium wymaga ich zmiany, powiedz to i zatrzymaj się.
- Zmieniaj tylko pliki potrzebne do tego zadania; inne problemy wypisz jako POZA ZAKRESEM.
- Komentuj DLACZEGO z datą 2026-09 przy nieoczywistej linii (niezmiennik 24).
