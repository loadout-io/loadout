# Z-24 — Jeden licznik wierszy strumienia

src/sections/run/io.ts ma cztery osobne liczniki stamp (start, ask, asARun, openChat), każdy od 1, więc rozmowa i bieg w jednym feedFor(folder) oraz każdy ponowny openChat dają zduplikowane id w jednej historii: duplikaty key w feed.tsx, toggle w model.ts rozwija cudzy wiersz, Answered rysuje się pod złym pytaniem (echo.ts przyznaje to w nagłówku). Wprowadź jeden monotoniczny licznik na moduł (nextStamp w src/state/run.ts) używany przez wszystkie cztery pompy; echo zostaje ujemne. Test: dwie pompy w jednej sesji + ponowny openChat — zbiór id bez powtórzeń (na starym kodzie powtórzenia); toggle po id rozwija właściwy wiersz.

## Kryterium akceptacji (z audytu 2026-09-02)

Rozmowa + bieg w jednej sesji + remont ekranu Run: żadnych dwóch wierszy o tym samym id; toggle rozwija właściwy wiersz.

## Źródła w audycie

F-1

## Reguły tego zadania

- Przeczytaj AGENTS.md; niezmienniki cytuj po numerze.
- Test z sekcji Test planu ma paść na starym kodzie zanim powstanie poprawka.
- Nie ruszaj harness/, checks/, scripts/, .claude/, AGENTS.md, docs/DECISIONS-LOCKED.md. Jeśli kryterium wymaga ich zmiany, powiedz to i zatrzymaj się.
- Zmieniaj tylko pliki potrzebne do tego zadania; inne problemy wypisz jako POZA ZAKRESEM.
- Komentuj DLACZEGO z datą 2026-09 przy nieoczywistej linii (niezmiennik 24).
