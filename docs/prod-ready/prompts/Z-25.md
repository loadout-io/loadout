# Z-25 — Lista procesów publikuje tylko zmiany; wiersze bez przerysowania

src/sections/run/rail/rail.tsx odpytuje list_processes co 1 s przez całe życie okna, a processes.ts publikuje NOWĄ tablicę nawet bez zmian, więc Run i cały Feed (do 2000 Message bez memo, markdown leksowany od nowa w answer.tsx, message.tsx, line.tsx) przerysowują się co sekundę. Publikuj tylko przy zmianie (porównanie pgid/alive/command/said), odpytuj tylko gdy held.length > 0 albo panel otwarty, memo(Message) i useMemo na leksowanie. Zdecyduj o wirtualizacji: @tanstack/react-virtual jest w package.json bez ani jednego użycia — albo użyj go w feed.tsx, albo zgłoś właścicielowi do usunięcia (package.json jest w deny). Testy: refreshStarted z identyczną odpowiedzią nie woła publish; licznik renderów Message stały przy tiku bez zmian.

## Kryterium akceptacji (z audytu 2026-09-02)

Przy pustej liście procesów startedThings nie zmienia referencji między tikami; Message nie renderuje się ponownie bez zmiany wiersza; decyzja: wirtualizacja albo usunięcie zależności.

## Źródła w audycie

F-2, R-10 (decyzja o react-virtual)

## Reguły tego zadania

- Przeczytaj AGENTS.md; niezmienniki cytuj po numerze.
- Test z sekcji Test planu ma paść na starym kodzie zanim powstanie poprawka.
- Nie ruszaj harness/, checks/, scripts/, .claude/, AGENTS.md, docs/DECISIONS-LOCKED.md. Jeśli kryterium wymaga ich zmiany, powiedz to i zatrzymaj się.
- Zmieniaj tylko pliki potrzebne do tego zadania; inne problemy wypisz jako POZA ZAKRESEM.
- Komentuj DLACZEGO z datą 2026-09 przy nieoczywistej linii (niezmiennik 24).
