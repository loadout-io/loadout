# Audyt 2026-09-04 — po pętli Z (main `81762fe7`)

Pełna wersja z dowodami, liczbami i tabelą kolejki: https://claude.ai/code/artifact/de679813-5ae4-40e7-8f1d-26148711c4a6
Poprzednik: `AUDIT-2026-09-02.md` (122 znaleziska; 33 z 34 zadań Z wlane, wszystkie trzy krytyczne
zamknięte i zweryfikowane w kodzie — patrz sekcja „Co zamknęła pętla Z" w artefakcie).

## Werdykt

- **Jeden workspace, jeden bieg naraz — niezawodne.** Graf, fan-in, pętle z sędzią, punkty
  kontrolne, dowód śmierci grup, udział budżetu, odzyskiwanie — przypięte testami (421 modułów
  w `tests/it`) i zweryfikowane w kodzie.
- **Kilka workspace'ów naraz — jeszcze nie** (A-1, A-2, A-3).
- **Koszt — dziurawy** (D-1, D-2, A-7). Zero wycieków pamięci.

Wagi: **W** wysokie · **Ś** średnie · **N** niskie.

## A · Silnik i bieg

- **A-1 (W)** `ipc.rs::stop_run` → `stop_every_live_run` zatrzymuje każdy żywy bieg we wszystkich
  workspace'ach. → Z-35
- **A-2 (W)** `continue_run`, `say_to_agent`, zamknięcie karty biorą `state.deps()` = bieg, który
  ruszył ostatni. → Z-35
- **A-3 (Ś)** `trigger_poll_permit`: trigger zajęty, gdy cokolwiek biegnie gdziekolwiek. → Z-43
- **A-4 (Ś)** Ciężka praca z Basha agenta (cargo, playwright) omija miejsce ciężkie — krok agenta
  jest zawsze `Ordinary`. → Z-45
- **A-5 (Ś)** `handoffs/`, `attachments/` zapisywalne przez każdy krok (`--add-dir`); rozjazd
  długości tylko `tracing::warn!` (21× w dzienniku). → Z-41
- **A-6 (Ś)** Workspace na Pulpicie bez zgody TCC: 42× „Operation not permitted" w
  `handoffs_of_run`, ekran milczy. → Z-42
- **A-7 (Ś)** Codex z modelem spoza `PRICES` jedzie poza sufitem. → Z-44

## B · Kontekst, skille, agenci

- **B-1 (Ś)** `LoadedFromTheFolder` nikt nie porównuje z tym, co bieg dał; obrót vendora między
  2.1.251 a 2.1.260 widać tylko ręcznie. → Z-47
- **B-2 (N)** ARCHITECTURE §8 mówi `agents/<slug>.json`, biblioteka ma 29 plików `.md`. → 0b.6

## C · Pamięć, dysk, sprzątanie

- **C-1 (W)** `urc-monorepo`: 12 drzew `work/s_*` z biegów sprzed znacznika izolacji (528 MB)
  i 99 gałęzi `loadout/*`; zamiatacz milczy; `keepLastRuns` nie zna gałęzi. → Z-46
- **C-2 (N)** Repo Loadouta: `target/debug/deps` 83 149 plików; `runs/` 165 katalogów / 547 MB
  (22 starsze niż 14 dni); `~/.loadout/loadout.db.bak-2026-09-04` 71 MB; `.loadout/scratch/t22`.
  → 0b.5, ZAMKNIĘTE (`cargo clean` zdjął 110 617 plików i 28,7 GiB).
  **SPROSTOWANIE 2026-09-04:** „5 martwych stanów w `.git/h/`" było błędem tego audytu.
  Nowe `h sweep` odmówiło wszystkim pięciu: worktree nie ma, ale każdy ma żywą gałąź
  z niewlaną pracą, więc `h list` nie kłamał. Stan bez worktree i bez gałęzi jest martwy;
  stan bez worktree, ale z gałęzią, to praca czekająca na decyzję.
- **C-3 (N)** Katalogi testów w `$TMPDIR` (`loadout-z01d-*`). → 0b.5

## D · Harness, pętla, koszt

- **D-1 (W)** Agenci w fazie implementacji: **1 475** komend Bash w 39 biegach (liczba
  poprawiona 2026-09-04 parserem; pierwsza wersja tego wiersza mówiła 4 019, bo liczyła też
  fazy planu i weryfikacji). Z tego `cargo clippy` **89×**, pełny albo `--lib` `cargo test`
  **13×**, `cargo build` **3×** — mimo zakazu w `implement.md`; `scripts/h check` 0×.
  `cargo check` 44× i zawężony `cargo test --test` 358× są legalne i zostają. Prompt jest
  miękki (niezmiennik 28). → 0b.1, ZAMKNIĘTE
- **D-2 (W)** Brak księgi kosztów: pole „koszt" w Dzienniku puste we wszystkich wierszach;
  Codex nie oddaje kosztu, Claude z `--json-schema` też nie. Pętla Z ≈ 844 USD po stronie
  Claude, z tego 254 USD na trzech biegach BLOCKED; od 28.08 2 240 USD / 159 biegów. → 0b.2
- **D-3 (Ś)** H-24: faza planu bez sesji (12,35 USD za nic na Z-22). → 0b.3
- **D-4 (Ś)** H-25: `ORACLE` nie zna `docs/ARCHITECTURE.md` i `docs/design/DESIGN.md`. → 0b.3
- **D-5 (Ś)** Reguły `Write(…)` w `settings.json` są martwe na CLI 2.1.260 (33 transkrypty
  z ostrzeżeniem); deny kryte bliźniakami `Edit`; ochrona wyroczni przed Bashem jest post-hoc.
  UWAGA: `checks/quick-permissions.sh` WYMAGA par Edit/Write — nie zdejmować bez zmiany
  `MUTATING` tam. → 0b.1 (hak), reszta zostaje
- **D-6 (Ś)** CI GitHuba nie widziało 162 commitów (`origin/main` w tyle, push w `deny`). → decyzja
- **D-7 (N)** Hak Stop biegnie przy każdym Stop każdej sesji w repo (do 600 s). → 0b.4
- **D-8 (N)** Z-34 stoi z werdyktem DZIAŁA na zapadce gęstości 52/49/60. → decyzja
- **D-9 (N)** `harness/README.md` i nagłówek `h.py` z nieaktualnymi liczbami. → 0b.6

## E · Okno i dokumentacja

- **E-1 (N)** `list_handoffs_inner` czyta wszystkie biegi przy każdym wejściu. → Z-49
- **E-2 (N)** `docs/STATUS.md` stoi na 2026-08-30; prawdą jest `prod-ready/PLAN.md`. → 0b.6
- **E-3 (N)** README opisuje `harness/` z czasów kontraktów i paragonów. → 0b.6

## L · Z eksportu diagnostyki i dwóch zrzutów właściciela (2026-09-04, workspace meetnotes)

- **L-1 (W)** Lider „zawiesza się": siedział 7+ min w jednym Bashu (`until grep …; do sleep 5;
  done`); CLI słało `tool_progress` co 30 s, pompa je porzuca, karta narzędzia powstaje dopiero
  z parą `tool_use`+`tool_result`; wiadomość człowieka (z obrazkiem 1 MB) była `delivered`, ale
  nic nie mówiło, że czeka za narzędziem. „Odpalił się po paru minutach" = koniec tego Basha.
  → Z-36 (widoczność), Z-40 (przerwanie)
- **L-2 (W)** Most nie ma `stop_run`, więc lider 1.09 zabił kroki sam: `kill -TERM -<pgid>` z
  `run.json` (dwa razy, pary `exit 143`/`turns 0` w odstępie 300 ms); Loadout uznał to za
  porażkę z `carry-on` i pojechał dalej (Final Plan 17 min, Combine 26 min na ubitych wynikach).
  → Z-39
- **L-3 (Ś)** „Statusy się nie zgadzają": po końcu biegu `io.ts` woła `nowRunning('', [], null)`,
  kafelki wracają do planu z pliku (`waiting`), nagłówek mówi „READY TO RUN · STARTED 16:2x",
  strumień mówi „QA · Done". → Z-37
- **L-4 (Ś)** Refleksja po biegu: `REFLECTION_BUDGET_USD = 0.08`, `context: []`,
  `error_max_budget_usd` po 6 turach; `reflection.why = nothing-came-back`; ekran milczy;
  „Learn from this run" nie uczy niczego. → Z-38
- **L-5 (N)** Zdanie pod polem rozmowy („…but only /run starts work") kłóci się z
  `--permission-mode bypassPermissions` i `--tools …Edit,Write,Bash…` lidera. → Z-50
- **L-6 (N)** `inputTokens`/`cachedTokens` znaczą co innego u Claude (302 / 25,9 M) i Codeksa
  (10,0 M ⊇ 9,4 M); kopie pętli, które nie biegły, eksport pokazuje jako `succeeded`; per-step
  `handoffs.total` = 0 przy 9 na biegu; 154-turowy krok czyta średnio 168 k tokenów na turę.
  → Z-48

## Fala 0b — wykonana 2026-09-04 wieczorem

Sześć pakietów ręką orkiestratora, cztery commity, pełna bramka zielona (403 s na zimno po
`cargo clean`, strażnicy 10/10). Zamknięte: D-1, D-2, D-3, D-4, D-7, C-2, C-3, B-2, E-2, E-3,
D-9. Dwie rzeczy warte zapisania, bo obie znalazła sonda, a nie przegląd:

1. **Hak `pre-bash` w pierwszej wersji miał trzy fałszywe alarmy** — odmawiał `grep clippy
   scripts/ci.sh` (odczyt bramki, nie jej odpalenie), `cargo test --test it -- <słowa filtru>`
   (zawężenie bez `::`) i `npx vitest run e2e` (katalog to też cel). Zobaczyła je dopiero sonda
   nad prawdziwym korpusem 1 475 komend, nie lektura. Wszystkie trzy siedzą teraz w tablicy
   przypadków, więc nie wrócą. Po poprawce: 105 odmów, każda uzasadniona.
2. **Księga kosztów w pierwszej wersji milczała o Codeksie** — `codex exec --json` nie wypisuje
   nazwy modelu ani razu, więc cena wychodziła `None` na każdym biegu Codeksa. Model bierze się
   teraz z tej samej konfiguracji, z której bierze go argv. Faza weryfikacji Claude'a zostaje
   poza sumą i księga mówi to wprost: idzie ze `--json-schema` bez `--output-format stream-json`.
   Zmierzone wstecz na z30…z34: 99,62 USD, i widać przy okazji, że ta sama praca u Codeksa
   kosztuje kilkakrotnie mniej niż u Claude'a.

## Decyzje właściciela (przed odpowiednim zadaniem)

1. Z-34: podnieść bazę gęstości do 52 (sufit 60) ręcznie z datowanym powodem i wlać, czy odrzucić.
2. Push 162 commitów na `origin/main` (CI, wydanie 0.2.4) czy CI zostaje dekoracją.
3. Planista domyślny: Codex, albo Claude z `LOADOUT_CLAUDE_EFFORT=high` dla fazy planu.
4. Z-45: pole `weight` na kroku (domyślne w prompcie) czy tylko zapis w dokumentacji.
5. Z-44: odmowa startu przy nieznanej cenie (domyślne w prompcie) czy stawka ostrożna.
6. Z-46: retencja gałęzi idzie za `keepLastRuns` (domyślne; przy 0 nic nie schodzi) — inna liczba?
7. Cudzy `cargo test --lib` z sesji Supersetu (4 h 38 min) obok żywego biegu — do Z-45 to ręka
   właściciela szereguje ciężkie procesy między aplikacjami.

## Metoda i granice

Wyłącznie odczyt; ani jednego `cargo`/`vitest` na `main`, bo przez cały pomiar trwał żywy bieg
Murmur-1 w meetnotes. Kwoty z transkryptów `runs/` (`result.total_cost_usd`); Codex niepoliczalny.
Nie zmierzone: `ci.yml` na runnerze, ⌘Q na żywym oknie, Windows.
